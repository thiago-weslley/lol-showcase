import { styled } from "styled-components";

export const LayoutContainer = styled.main`
  > img {
    z-index: 99;
    position: absolute;
    left: 2%;
    top: 2%;
    user-select: none;
    width: 15rem;
  }
`;
