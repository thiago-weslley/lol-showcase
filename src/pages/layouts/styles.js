import { styled } from "styled-components";

export const LayoutContainer = styled.main`
  > img {
    z-index: 99;
    position: absolute;
    left: 20px;
    top: 20px;
    user-select: none;
    width: 15rem;
  }
`;
